<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No <uses-permission> is declared here on purpose, and adding one would
         make the app worse rather than safer:

         * CAMERA — image_picker takes stills through MediaStore.ACTION_IMAGE_CAPTURE,
           which hands off to the installed camera app and needs no permission of
           ours. Worse, ImagePickerUtils.needRequestCameraPermission() checks the
           *manifest*, so declaring CAMERA makes image_picker start demanding a
           runtime grant it does not otherwise need, and Play infers
           <uses-feature android:name="android.hardware.camera" android:required="true">
           from the permission, filtering the app off camera-less devices.
         * READ/WRITE_EXTERNAL_STORAGE and READ_MEDIA_IMAGES — reading a photo goes
           through the system photo picker and saving a PDF goes through the Storage
           Access Framework (flutter_file_dialog -> ACTION_CREATE_DOCUMENT). Both hand
           back a URI the user chose; broad storage access is exactly what they exist
           to avoid.

         No <queries> entry is needed either: the plugins launch their intents with
         startActivityForResult rather than resolving them first. -->
    <application
        android:label="Resivo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
