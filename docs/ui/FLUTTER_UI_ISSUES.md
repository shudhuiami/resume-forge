# Flutter UI Issues

Defect log for ResumeForge Flutter UI work. Populated by the
`flutter-ui-specialist` agent and the `/flutter-ui-workflow` skill.

## Severity

| Level  | Meaning                                             |
| ------ | --------------------------------------------------- |
| **P0** | Screen unusable / crash / critical flow inaccessible |
| **P1** | Major workflow or interaction broken                |
| **P2** | Responsive / usability / theme problem              |
| **P3** | Visual polish                                       |

## Type

`overflow` · `responsive` · `theme` · `keyboard` · `safe-area` · `navigation` ·
`state` · `accessibility` · `interaction` · `performance` · `dead-control` · `polish`

## Device

Record the device the issue was **observed** on, including the verification platform —
e.g. `390x844 (web/chrome)`, `Pixel 6 emulator (android)`. Never record a device that
was not actually used.

## Status

`open` · `in-progress` · `fixed` · `wont-fix` · `cannot-reproduce`

---

## Issues

| ID | Module | Screen | Severity | Type | Device | Expected | Actual | Evidence | Status |
| -- | ------ | ------ | -------- | ---- | ------ | -------- | ------ | -------- | ------ |
| UI-001 | editor | EditorScreen | P2 | tooling | 390x844 (web/chrome) | A harness entry point boots straight into the editor so its pixels can be inspected at each viewport | `lib/main_harness.dart` rendered a blank page for every SCREEN value, including ones that render fine from `main.dart`. Engine booted (`flutter-view` present) but no frame painted, and no error surfaced through `FlutterError.onError`, `PlatformDispatcher.onError`, or `ErrorWidget.builder` | Harness deleted rather than left as a silently broken dev tool. Editor renders correctly in widget tests | open |
| UI-002 | editor | EditorScreen | P2 | tooling | 390x844 (web/chrome) | Editor pixels visually verified at 360/390/430/768 | **RESOLVED.** Now verified at all four viewports plus the Preview tab. The blocker was never the harness: `1 << 32 == 0` on dart2js made resume creation throw, so the two-tap path into the editor died every time. Original note follows. **Not visually verified.** Reaching the editor needs two taps and Flutter renders to a canvas: coordinate clicks are flaky and enabling the semantics tree injects DOM overlays that swallow pointer events. Layout is instead verified by widget tests at all four viewports with realistic, pathological, and empty content | `test/screens/editor_responsive_test.dart` plus captured screenshots at 360/390/430/768 | fixed |
| UI-003 | preview | PdfPageView | P2 | state | n/a | Preview tab rasterization verified on-device | `Printing.raster` is platform-bound and the preview tab was never reached in a browser session. Raster sizing maths is unit-tested; the actual pixels are unverified | `test/render/pdf_raster_test.dart` covers DPI/clamping only | open |
| UI-004 | app | all | P3 | performance | n/a | Preview latency measured on a low-end Android profile | No Android SDK, emulator, or `adb` in this environment. Task 9 remains unmeasured; template mass-production proceeds on the untested assumption that the debounce-and-rasterize loop is fast enough on budget hardware | — | open |
| UI-005 | gallery | GalleryScreen | P3 | state | 390x844 (web/chrome) | An empty catalog shows an explanation rather than a blank screen | `GridView.builder` over `resumeTemplates` renders nothing at all when the list is empty. Unreachable today — the registry is a non-empty `const` list — so an empty state here would be untestable dead code, but a runtime-loaded catalog would expose it | `lib/templates/registry.dart` guarantees at least `AuroraTemplate()` | open |
| UI-006 | gallery | _Thumbnail | P3 | polish | 768x1024 (web/chrome) | Thumbnails are rasterized at the size the tile actually renders at | Every thumbnail rasterizes at a fixed `logicalWidth: 200, pixelRatio: 2` and the cache is keyed on template id alone, so a 234px tablet tile upscales a 400px raster and reads slightly soft. Fixing it needs a width-aware cache key, which is a render-layer decision, not a layout one | `lib/screens/gallery_screen.dart` `_ThumbnailState._load`; visible when comparing 360 and 768 screenshots | open |
| UI-007 | resume-list | _ResumeTile | P3 | polish | 390x844 (web/chrome) | A resume edited more than a month ago reads as a date | `_relative()` falls back to `2026-07`, so the row says "edited 2026-07" — a stamp rather than a phrase, and ambiguous about the day Fixed: past a month the row now reads "Edited on 24 Jan 2026". Month names are spelled out in the screen rather than pulled from `intl` -- the app carries no localisation and adding a package to format three words would be the larger change | `lib/screens/resume_list_screen.dart` `relativeEditedAt`; `test/screens/resume_list_layout_test.dart` | fixed |
| UI-008 | gallery, resume-list | GalleryScreen, ResumeListScreen | P2 | tooling | 390x844 (web/chrome) | Both screens confirmed on a real Android or iOS device | Verified on Flutter Web in Chromium only — no Android SDK, emulator, `adb`, or iOS simulator exists here. Touch behaviour, platform scroll physics, real safe-area insets (notch, gesture bar) and native text scaling are therefore unverified for both screens | Screenshots at 360/390/430/768 plus landscape; `test/screens/gallery_layout_test.dart` and `test/screens/resume_list_layout_test.dart` | open |
| UI-009 | preview | PdfPageView / PreviewEngine | P2 | state | 390x844, 360x800 (web/chrome) | Typing and switching tabs never fails a render | An intermittent render/raster failure puts the preview into its stale state ("Showing the last good preview") and it stays there until retried. Seen three times across browser sessions, always shortly after a tab switch during an in-flight render. Not reproduced under instrumentation (a temporary `debugPrint` on the controller's error path logged nothing across six provocation cycles), so the exception text is still unknown. The UI now survives it — the last good page is kept and Retry works — but the underlying failure is a render-layer defect and is unfixed. Suspects: overlapping `Printing.raster` calls through pdf.js on web, and `ResumeFonts.loadBytes` using `.buffer.asUint8List()` without offset/length, which returns the whole backing buffer if the web bundle hands back a view | Screenshots `before-390x844-c-preview-busy.png`, `v2-360x800-g-export-progress.png` | open |
| UI-010 | preview, export | ResumePreview | P2 | state | n/a | A resume that runs to two pages can be seen and understood | `PdfRaster.firstPageToPng` renders page index 0 only, and nothing in the UI says a second page exists. A user whose content overflows page one gets a preview that silently disagrees with the exported PDF, which breaks the "what you see is what you get" guarantee. Fixing it needs a page count out of the rasterizer plus a pager in the preview — a render-layer change, not a layout one | `lib/render/pdf_raster.dart` `firstPageToPng`; `pages: const [0]` | open |
| UI-011 | preview, export | EditorScreen preview tab, export action | P2 | tooling | 390x844 (web/chrome) | Preview and export confirmed on a real Android or iOS device | Verified on Flutter Web in Chromium only — no Android SDK, emulator, `adb`, or iOS simulator here. Two things are therefore unverified where they matter most: `Printing.sharePdf` opens a real share sheet (on web it silently downloads, so the dismissed-vs-shared distinction was never exercised), and PDFium rasterization on device (the web path is pdf.js). Storage-permission and out-of-space failure messages have never been seen fire | Screenshots at 360/390/430/768 plus landscape; `test/screens/editor_export_test.dart` takes the failure path only | open |
| UI-012 | export | EditorScreen | P3 | interaction | 390x844 (web/chrome) | A finished export is acknowledged | On success nothing is shown, on the assumption that the platform share sheet is its own confirmation. That holds on mobile but not on web, where the file downloads with no in-app trace and the progress snackbar has already closed. Left silent rather than risking a snackbar racing a native sheet on the platforms that were not testable here | `lib/screens/editor_screen.dart` `_export`; `ExportStatus.shared` has no UI branch | open |
| UI-013 | export | PdfExport | P3 | dead-control | n/a | Every shipped capability is reachable | `PdfExport.printDocument` — the system print / save-as-PDF path — has no entry point anywhere in the UI. It is tested but unreachable, so on desktop web there is no "print" and on Android no "save to Files" short of the share sheet. Adding a second export action is a product decision, not a layout fix | `lib/render/pdf_export.dart`; no call sites outside `test/render/pdf_export_test.dart` | open |
| UI-014 | preview | ResumePreview | P3 | polish | 390x844 (web/chrome) | The busy marker does not sit on top of the document | The "Updating" pill is anchored inside the page's top-right corner, which is exactly where most templates put the header, so mid-render it covers the last few characters of the person's name. There is ample empty chrome above and below the page to move it into, but that means restructuring the preview from a single letterboxed page into a page-plus-gutter layout | Screenshot `after-390x844-c-busy.png` | open |

| UI-015 | editor | _EditorForm | P2 | responsive | 768x1024 (web/chrome) | Single-line fields stop at a readable measure on a tablet | Form stretched edge to edge across ~736px, reading as an admin table rather than a document editor. Found only by looking at the running app; every widget test passed because nothing overflowed | Fixed: 640px max form width, matching the resume list. `test/screens/editor_responsive_test.dart` covers both tablet and phone | fixed |

| UI-017 | app | all | P2 | tooling | n/a | ~~The light "soft glass" palette is confirmed on a real device~~ **Superseded by UI-029: that palette no longer exists.** | Re-skin verified by rendering each screen to PNG from `flutter test` and inspecting the images, plus the full suite. Two things that path cannot show: `flutter_test` sets `debugDisableShadows = true`, so every soft shadow was drawn as a hard 1px outline of the shadow colour rather than as a diffuse falloff — the shadow *placement* is verified, its softness is not; and text renders in the test block font, so type colour is verified but weight, tracking and the Inter fallback are not. No emulator or device session was available | Screens captured at 390x844 and 768x1024; harness deleted after use | wont-fix |

| UI-018 | tooling | — | P3 | tooling | n/a | The suite is deterministic | `test/state/editor_controller_test.dart` "setTemplate ignores an unknown template id" failed once in a full parallel run and passed in every other run, including alone. Its `settle()` helper waits on a real 50ms wall clock to outlast the controller's render debounce, so a loaded machine can beat it. Timing-based, unrelated to the theme change; the fix is a fake clock or an explicit completion signal on the controller | `test/state/editor_controller_test.dart:111` | open |

| UI-016 | editor, export | EditorScreen | **P1** | state | n/a | A resume never silently loses a job | Templates drop content that does not fit rather than flowing to a second page, and measured capacity is only 4-7 roles depending on the design — inside a normal career. Content vanished with no warning, no extra page, and no visible gap, and the preview agreed with the export because both were truncated identically. **Partly fixed:** export now detects the loss and asks the user to confirm. The underlying one-page limit remains; UI-010 (multi-page) is the real fix | `lib/render/truncation_check.dart`; measured capacity per template recorded in the commit message. Warning re-confirmed on the dark palette: the banner renders as an error-container band under the tab bar and survives 360x800 at 2x text, wrapping to four lines with no overflow | in-progress |

| UI-019 | editor | ResumeTextField | P2 | state | 390x844 (flutter_test render) | A field shows whatever the model currently holds | Every editor field is an uncontrolled `TextFormField` seeded with `initialValue`, which Flutter reads exactly once — `FormFieldState.didUpdateWidget` does not re-seed it. Per-keystroke editing is unaffected, but any code that replaces the document wholesale updates the model, the preview and storage while the visible text stays stale. Worked around, not fixed: `EditorScreen` keys `_EditorForm` on a `_formRevision` counter that the sample/clear/undo actions bump, forcing the subtree to rebuild. The next feature that replaces data from anywhere else will hit the same trap. The real fix is controller-backed fields, which changes how every field in the form is wired Fixed: `ResumeTextField` and `MonthYearField` are stateful and own a `TextEditingController`, re-seeded from `didUpdateWidget` only when the model and the visible text actually differ, so typing never moves the caret. `_formRevision` is gone. Fixing it uncovered a live defect the workaround had hidden: switching "I currently work here" on clears `endDate` in the model, and the seeded field kept showing the old date while the PDF printed "Present" | `lib/widgets/form_fields.dart` `_ModelBackedField`; `test/widgets/form_fields_test.dart`, `test/screens/editor_fields_test.dart` | fixed |

| UI-020 | editor, resume-list | EditorScreen, ResumeListScreen | P3 | polish | n/a | One destructive-confirmation component, used everywhere | The same dialog — title, sentence of consequence, `Cancel` text button, error-coloured `FilledButton` — is now hand-built in three places: `_confirmDelete` in the resume list and `_confirmDestructive` in the editor (shared by load-sample and clear). Tone and shape match today because they were copied; nothing keeps them matching. `lib/screens/resume_list_screen.dart` was off-limits during this task, so extracting a shared widget would have left the list on its own copy anyway Fixed: one `confirmDestructive()` in `lib/widgets/confirm_dialog.dart`, used by all three call sites. A dismissed dialog answers false, which is now asserted rather than assumed | `lib/widgets/confirm_dialog.dart`; `test/widgets/confirm_dialog_test.dart` | fixed |

| UI-021 | gallery | GalleryScreen | P2 | tooling | 390x844, 360x800, 430x932, 768x1024, 800x360, 320x640 (flutter_test render) | The redesigned cards are seen holding real rasterized pages | Every captured tile shows the loading placeholder instead: `Printing.raster` is platform-bound and `flutter_test` has no implementation (`MissingPluginException: rasterPdf on channel net.nfet.printing`), so the card frame, chips, header, badge, spacing and type were all verified against a spinner-filled A4 box. How thirteen *actual* light documents read side by side inside the new white cards — the exact "wall of white pages" complaint — is still unverified. Needs the device pass or a browser session | Screenshots captured through a temporary golden harness at six viewports plus 2x text scale, then deleted; `test/screens/gallery_layout_test.dart` | open |
| UI-022 | gallery | GalleryScreen | P3 | responsive | 800x360 (flutter_test render) | A landscape phone shows at least one complete design without scrolling | The heading, the promise line and the chip row take roughly 45% of a 360px-high viewport, leaving only the top third of the first card row visible. The heading already steps down to `titleLarge` below 500px of height, which was worth ~40px. Genuinely fixing it means either floating the header away on scroll or capping the thumbnail height in short viewports, and an A4 thumbnail that is not A4 is worse than a scroll | `lib/screens/gallery_screen.dart` `_GalleryHeader.compact` | open |
| UI-023 | gallery | GalleryScreen | P3 | state | 390x844 (flutter_test render) | The design currently in use is findable while a filter is active | Filtering to a category the selected design is not in scrolls it out of the list with nothing left saying what is selected — the header count reports "2 of 13 designs" but not that your design is among the eleven hidden. Deliberate: auto-switching the chip to the selected design's category on open would hide the rest of the catalog on a screen whose whole job is browsing it. A "your design" marker on the chip row would fix it without that cost Fixed: when a filter hides the design in use, a note under the chip row names it and its category and offers "Show it", which switches the filter to that category. An offer, not an action -- the catalog is never taken away from someone browsing it | `lib/screens/gallery_screen.dart` `_HiddenSelectionNote`; `test/screens/gallery_layout_test.dart` "when a filter hides the design in use" | fixed |

| UI-024 | editor | EditorScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 (flutter_test render) | The restructured editor form is seen on a real device | The section-card layout, icon tiles, entry pills and the new soft-filled fields were verified by rendering the screen to PNG from `flutter test` and reading the images, plus the widget suite. That path cannot show: real touch behaviour on the new full-width add buttons, platform scroll physics, the actual system keyboard (insets were faked), or native text scaling. Shadow softness is also unverified in the suite — `flutter_test` forces `debugDisableShadows`, and the capture harness had to unset it per test to see a diffuse falloff at all | Captures at five viewports plus 2x text scale and keyboard-open, taken through a temporary golden harness, then deleted; `test/screens/editor_responsive_test.dart` | open |
| UI-025 | editor | ResumeTextField | P3 | keyboard | 390x844 (flutter_test render) | A focused multi-line field is fully clear of the keyboard | Focusing a tall field (Summary, 5 lines) scrolls the caret into view but leaves roughly the last 25px of the field under the keyboard line, because `EditableText` scrolls the caret rather than the field's bounds. Typing is unaffected — the caret is always visible — so this is polish, not a blocker. Single-line fields are asserted clear of the keyboard | `test/screens/editor_responsive_test.dart` "a focused field further down is scrolled clear of it"; measured 533px against a 508px keyboard line for Summary | open |
| UI-026 | editor | _EditorForm | P3 | polish | n/a | Keyboard clearance is expressed once | The form list adds `MediaQuery.viewInsetsOf(context).bottom` to its bottom padding, but `Scaffold` already strips the bottom view inset from its body's `MediaQuery` when `resizeToAvoidBottomInset` is on (the default), so that term is always 0 and the comment above it describes something the Scaffold is doing. Harmless and defensive — left alone rather than churned during a layout pass | `lib/screens/editor_screen.dart` `_buildForm` padding; `packages/flutter/lib/src/material/scaffold.dart:3034` `removeBottomInset: _resizeToAvoidBottomInset` | open |

| UI-024 | resume-list | ResumeListScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 (flutter_test render) | The restructured home is seen on a device | Verified by rendering the screen to PNG from `flutter test` with the real Inter fonts loaded, then inspecting the images. That path cannot show three things: `flutter_test` disables shadow softness, so every card and tile drew as a hard dark ring rather than the diffuse `AppTokens.shadowSoft` falloff; the Material icon font is absent, so every glyph captured as a tofu box (icon *choice* is unverified, only its size and position); and real touch, scroll physics and safe-area insets are untested. No emulator, `adb` or simulator in this environment | Captures at all five viewports plus 2x text scale, harness deleted after use; `test/screens/resume_list_layout_test.dart` | open |
| UI-025 | resume-list | _ResumeCard | P3 | polish | n/a | A row shows the design it is on, not just its name | Rejected per-row rasterized first-page thumbnails after measuring: a PDF build costs 26-68ms per document on a warm desktop VM (`PdfRenderer.build`, fonts cached), before the `compute` isolate spawn, the font-byte copy across it, and a PDFium raster that cannot leave the platform thread. Unlike the gallery's thirteen fixed designs over one sample document, every row here is unique content and every autosave bumps `updatedAt`, invalidating the row the user just returned from — all of it on the cold-start screen, unbounded in row count. The card shows an A4-shaped initials plate, a design chip, a content summary and the edit time instead. A real thumbnail needs a disk cache keyed by document id and revision, written off the render path — a data-layer change | `lib/screens/resume_list_screen.dart` `_ResumeCard`; benchmark run against `resumeTemplates.take(6)` | open |
| UI-026 | theme | AppTheme.cardTheme vs AppTokens | P3 | polish | n/a | One shadow language for panels | `AppTokens.shadowSoft` documents a two-layer cool-tinted diffuse shadow as the structural token that separates a panel from the background, but `cardTheme` separates cards with Material `elevation: 2` and `shadowColor` instead. Both are in use on the home screen (cards via elevation; nothing else needs the token now that the quick-action tiles are `Card`s). They are different falloffs and cannot be kept in step. The fix is `elevation: 0` on the card theme plus the token shadow applied by whatever wraps the card — a theme change, which was owned by another agent this round Fixed in two rounds. The dark re-tone deleted the shadow tokens outright and flattened `cardTheme`, `dialogTheme`, `bottomSheetTheme`, `snackBarTheme` and the FAB to `elevation: 0`, separating panels by a tint step plus a hairline. What that round missed: `popupMenuTheme` was never set, so the editor overflow menu still drew Material default `elevation: 8` with a black shadow -- the same two-languages defect surviving in the one surface nobody had looked at. Found by rendering the menu, not by reading the theme. Now flat, on the raised tier, with a visible outline: a popup has no scrim and no screen behind it, so it is the one raised surface that needs an edge of its own | `lib/theme/app_theme.dart` `popupMenuTheme`; `test/theme/contrast_test.dart` "no surface separates itself with a shadow" now asserts per surface | fixed |
| UI-027 | resume-list | ResumeListScreen | P3 | polish | 390x844 (flutter_test render) | The product is named on the screen a first-run user sees | Removing the `AppBar` moved "ResumeForge" into the greeting header, which only renders once at least one resume is saved. On first run the empty state shows the hero, "No resumes yet" and the CTA — and the app name appears nowhere. Adding an eyebrow above the hero pane costs height that the landscape-at-2x case has already run out of, so it was left alone rather than fought with Fixed: one `Wordmark` widget, used by the greeting and by the empty state, where it sits as an eyebrow above the hero. Its height is reserved in `_heroCopyReserve`, so the orb gives the space up rather than the copy -- the landscape-at-2x case still scrolls to its CTA | `lib/screens/resume_list_screen.dart` `Wordmark`; `test/screens/resume_list_layout_test.dart` | fixed |
| UI-028 | resume-list | ResumeListScreen | P3 | interaction | 390x844 (flutter_test render) | One create action per screen | "New resume" is now both a quick-action tile and the extended FAB — the duplication the empty state deliberately avoids. Kept because the tile row was specified to include create and the FAB is the only create action that survives scrolling, but two identical calls to action on one screen is a smell. Either the FAB becomes icon-only, or the tile row drops to browse-only Fixed: the FAB is icon-only with a "New resume" tooltip. The tile keeps the words, the FAB keeps the job the tile cannot do. It also stops the create action taking a third of a 360px screen at 2x text, since a FAB is a fixed graphic and an extended one grows with its label. **Superseded by UI-060:** icon-only was not enough -- the user reported the two controls again -- so the tile is gone and the FAB is the only create action | `lib/screens/resume_list_screen.dart` `build`; `test/screens/resume_list_layout_test.dart` "the create action exists exactly once" | fixed |

Found by looking at rendered pixels during the dark-palette pass, not by any
test. Numbered from UI-029; note that UI-024, UI-025 and UI-026 were each issued
twice by concurrent agents above, so those three IDs are ambiguous.

| UI-029 | app | all | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render) | The dark colour-tinted palette is confirmed on a real device | Verified by rendering all three screens plus both dialogs, both snackbars, the overflow menu in each state, the truncation banner and every preview state to PNG from `flutter test`, with the real Inter cuts and the SDK Material icon font loaded, then reading the images. That path still cannot show: real touch, platform scroll physics, true safe-area insets, the system keyboard (insets were faked), native text scaling, and how a saturated dark palette reads on an OLED panel at low brightness. No emulator or device in this environment | Captures taken through a temporary harness at five viewports, deleted after use | open |
| UI-030 | theme, editor | inputDecorationTheme | P2 | accessibility | 390x844 (flutter_test render) | A switched-off field looks switched off | The disabled end date -- the "I currently work here" case -- was indistinguishable from the live field beside it. Setting a flat `labelStyle` colour in `inputDecorationTheme` overrides Material own disabled resolution, so the label was painted at full `onSurfaceVariant`: measured 1.00:1 from the enabled state. The fill does not change either, so the only cue was the underline dropping to the hairline. Fixed: label, hint and helper resolve `WidgetState.disabled` to a faded ink (2.93:1 from the enabled label, still 3.04:1 on the fill so the field can be identified), and the field now says `Shows "Present"` rather than only refusing the tap | `lib/theme/app_theme.dart` `_fieldInk`; `test/theme/contrast_test.dart` "a disabled field reads as disabled", `test/screens/editor_fields_test.dart` | fixed |
| UI-031 | gallery | _TemplateCard, _Thumbnail | P3 | theme | 390x844, 768x1024 (flutter_test render) | A thumbnail is a page inside a card, not the card again | The selected card and the thumbnail placeholder both took `surfaceContainerHighest` -- literally the same colour, 1.00:1 -- so on the one card the user is most likely to be looking at, the page had no fill of its own and read as an empty bordered rectangle. Affects the loading state and the "Preview unavailable" failure state, which is the persistent one. Fixed: the selected card drops to `surfaceContainerHigh`, restoring a card / selected-card / page ladder where each tier is a step above the last. The 2px accent ring and the "Selected" pill carry the selection cue regardless | `lib/screens/gallery_screen.dart` `_TemplateCard`; `test/screens/gallery_layout_test.dart` "card, selected card and page placeholder are three distinct tiers" | fixed |
| UI-032 | editor | _SkillSlider | P3 | responsive | 360x800 at 2x text (flutter_test render) | A rating word is readable at any text size | The word beside a skill slider had a flat 68px box -- a fixed graphic allowance handed to a line of copy. At 2x "Strong" did not fit, and being a single word it could not wrap: it broke mid-word into "Stron / g" and the second line rode up over the slider above. No `RenderFlex` overflow was thrown, so the responsive suite passed it at every viewport. Fixed: the box is measured from the widest label at the live text scale, capped at 45% of the row so the slider stays aimable, and the label ellipsizes on one line if the cap ever bites | `lib/screens/editor_screen.dart` `_SkillSlider._labelWidth`; `test/screens/editor_fields_test.dart` "stays on one line at double text size" | fixed |
| UI-033 | preview | ResumePreview | P3 | polish | 390x844, 800x360 (flutter_test render) | Chrome floated on the page is not clipped by it | The busy pill and the stale banner were inset by `spaceSm` (8) inside a page clipped to `radiusSm` (12), so their outer corners fell inside the page corner arc and `Clip.antiAlias` shaved them to a flat edge -- a rounded pill with one square corner, which reads as a rendering fault. Fixed: inset by `spaceMd`, which is the corner radius. **Superseded by UI-054**: the page no longer has a corner radius, so `spaceMd` is now a plain margin and the two values are no longer coupled | `lib/widgets/resume_preview.dart` | fixed |
| UI-034 | export | TruncationReport.describe | P3 | polish | 390x844 (flutter_test render) | The warning is grammatical | The banner reads "Some of your **projects does** not fit on the page" -- the singular verb is used for every section name, and three of the four ("projects", "skills", "education") are not singular. Seen on the rendered banner. Not fixed: `lib/render/` was outside this task's file ownership, and it is a copy change rather than a layout one | `lib/render/truncation_check.dart` `describe()` | open |
| UI-035 | resume-list | _DesignPlate | P3 | tooling | n/a | A test seam is reachable | `_DesignPlate.initialsFor` is annotated `@visibleForTesting` but sits on a private class, so nothing outside the library can call it -- the annotation is decoration and the function is covered only indirectly, through the rendered card. `relativeEditedAt` was moved to the top level for exactly this reason; this one was left alone because its indirect coverage is real | `lib/screens/resume_list_screen.dart` `_DesignPlate.initialsFor` | open |

Brand, launcher icon and startup. Numbered from UI-040 to leave room for the
screen work that was in flight in the same round.

| UI-040 | brand | launcher | P2 | polish | n/a | The launcher shows the product's name under the product's icon | `android:label` is `resume_forge` in `AndroidManifest.xml` and `CFBundleName` is `resume_forge` in `Info.plist`, so on the home screen the new icon is captioned with a package identifier. iOS already carries `CFBundleDisplayName` = "Resume Forge", which is right but should be "ResumeForge". Not fixed: the manifest and the plist are not generated icon output and were outside this task's ownership | `mobile/android/app/src/main/AndroidManifest.xml` `android:label`; `mobile/ios/Runner/Info.plist` `CFBundleName`, `CFBundleDisplayName` | open |
| UI-041 | brand | SplashScreen, StartupFailureScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render) | The icon and the splash are seen on a real device | Verified by rendering both screens to PNG from `flutter test` with the real Inter and Material icon fonts loaded, and by compositing the generated Android layers under circle/squircle/square masks at 48/72/108/192px and reading the images. That path cannot show the three things that matter most here: the OS launch window handing off to Flutter's first frame (the mark and the background colour were matched by *measurement* — 102.0x144.5dp against 102.2x144.5dp, and #171233 in four config files — never observed), the Android 12+ platform splash API, and a themed (Android 13 monochrome) icon on a real wallpaper palette. No emulator or device in this environment | `assets/brand/`; `test/brand/brand_assets_test.dart`; captures taken through a temporary harness at five viewports, then deleted | open |
| UI-042 | brand | SplashScreen, StartupFailureScreen | P3 | polish | n/a | One message-pane component, used everywhere | Both startup screens hand-roll the same "scroll-if-it-must, centre-if-it-fits" pane and the same rounded icon plate that `resume_list_screen.dart` already has as `_MessagePane` and `_IconTile`. Those are private to a file this task could not touch, so extracting a shared widget would have left the list screen on its own copy anyway — the same trap UI-020 records for the confirm dialog. Three copies of the pane now exist | `lib/screens/splash_screen.dart` `StartupFailureScreen`; `lib/screens/resume_list_screen.dart` `_MessagePane`, `_IconTile` | open |
| UI-043 | brand | native splash (iOS) | P3 | polish | n/a | Nothing in the launch storyboard is white | `LaunchScreen.storyboard`'s root view keeps `backgroundColor` white (`red=1 green=1 blue=1`). It is completely covered by the full-bleed `LaunchBackground` image view, so it is invisible in practice — but it is a white plate one constraint change away from being seen on a dark-only app. The storyboard is regenerated by `flutter_native_splash:create`, so a hand edit would not survive | `mobile/ios/Runner/Base.lproj/LaunchScreen.storyboard` | open |
| UI-044 | brand | native splash (Android 12+) | P3 | polish | n/a | The platform splash icon uses the animation slot it is given | `windowSplashScreenAnimatedIcon` points at a static PNG. Android 12+ will play an `AnimatedVectorDrawable` there for up to 1000ms, which would let the content bars write themselves *before* Flutter boots rather than after. Deliberate for now: the mark's entrance already plays in Flutter, an AVD would have to be authored and kept in step with `ForgeMark` by hand, and two animations of the same mark that disagree is worse than one that starts a beat later | `mobile/android/app/src/main/res/values-v31/styles.xml`; `flutter_native_splash.yaml` `android_12` | open |

Matte-black re-tone of the icon and splash. UI-045 onwards.

| UI-045 | brand | native splash → SplashScreen | P3 | responsive | 360x800, 390x844, 430x932, 768x1024 (measured) | The mark does not change size at the native-to-Flutter handoff | The native splash image is a fixed 144.00x102.00dp page at every density, but `ForgeMark.heightFor` sizes the Flutter mark from the viewport, so the two agree only at the reference width. Measured frame-0 page heights: 133.20dp at 360 wide (mark *shrinks* 7.5% at the handoff), 144.30dp at 390 (0.30dp out, the design point), 159.10dp at 430 (+10.5%), 176.00dp at 768 (+22%). Pre-existing and untouched by the palette work — a fixed-dp OS image cannot follow a responsive mark. The fix is to pin the Flutter frame-0 page to a fixed 144dp and let only the *resting* size scale, which changes the entrance on every phone that is not 390dp wide | `mobile/lib/screens/splash_screen.dart` `ForgeMark.heightFor`, `_markWidthShare`; `mobile/assets/brand/generate_brand.py` `PAGE_SPLASH` | open |
| UI-046 | brand, theme | brand assets vs AppTheme | P2 | theme | n/a | The baked icon palette and the Dart palette agree | The rasters, `flutter_native_splash.yaml` and `values/colors.xml` are regenerated at the agreed matte palette (base `#0E0E10`, mark `#D8A657`); `lib/theme/app_theme.dart` still carries `#171233` / `#B9A5FF` because it is owned by a concurrent agent. Two tests in `test/brand/brand_assets_test.dart` are red by design and name the exact drift — that is the guard working, not a defect in the assets. They go green the moment the theme lands `surface: #0E0E10` and `primary: #D8A657`; nothing else needs regenerating. Related: the splash mat takes `colorScheme.surfaceContainerHigh`, so how visible it is depends on where that tier lands — if the panel tiers end up within a hair of the base the mat disappears and the flat mark stands alone, which is an acceptable outcome rather than a broken one | `mobile/test/brand/brand_assets_test.dart` "the baked palette still matches Dart"; `mobile/lib/theme/app_theme.dart` | fixed |
| UI-047 | brand | SplashScreen, launcher icon | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render) | The matte icon and splash are seen on a real device | Verified by rasterising `ForgeMark` and the whole splash through `flutter test` and reading the pixels back, and by compositing the regenerated adaptive layers under circle, superellipse-squircle, rounded-square and full-square masks at 48px and 260px plus both Android 13 themed tints. Not observed: the OS launch window handing off to Flutter's first frame, the Android 12+ platform splash, a themed icon on a real wallpaper palette, and how `#D8A657` on `#0E0E10` reads on an OLED panel at low brightness. An emulator was attached (`emulator-5554`, API 37) but this task was explicitly asked not to launch the app — the device pass, including a cold-launch capture, is the requester's | `mobile/assets/brand/`; `mobile/test/brand/brand_assets_test.dart`; `mobile/test/screens/splash_screen_test.dart` "paints the mark in flat colour, not a ramp" | open |

Matte-black re-tone of the app shell. UI-048 onwards.

| UI-048 | gallery | _TemplateCard, _PageFrame, _Thumbnail | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 (flutter_test render) | Thirteen white A4 pages on matte black do not glare | The one thing the palette change most needed looked at, and the one thing the render path cannot show. `PdfRaster.firstPageToPng` never completes under `flutter test` -- the grid captures with all thirteen cells still on the `surfaceContainerHighest` placeholder and an amber spinner -- so the card frame, the category tint, the 2px accent ring and the placeholder state are all verified, and the actual glare of a real rasterised page inside a tinted card is not. What *was* verified with a real white page is the preview's A4 sheet, which carries the identical `outline` mat ring on the identical base: 3.40:1 from the paper, 5.68:1 from the surface. Needs a device pass on the gallery specifically | `mobile/lib/screens/gallery_screen.dart` `_PageFrame`; `mobile/test/screens/gallery_layout_test.dart` "the placeholder is neither paper nor a dark template page" | open |
| UI-049 | theme | ColorScheme.error | P3 | accessibility | n/a | Destructive copy is AA wherever it appears | The measured error accent `#E4715B` clears 4.5:1 on all six neutral tiers (5.39--6.49:1) but reaches only 3.73--4.52:1 on the eight card tints, so bare error *text* on a tinted card cannot be AA on this palette. Not a live defect: no field in this form validates, the overflow menu is a popup on `surfaceContainerHigh`, and the delete controls are icon buttons in `onSurfaceVariant` -- the previous palette's test asserted 4.5:1 on its panel tints for a case that never existed. Encoded instead as a rule with two tests behind it: destructive copy lives on the neutral tiers, and on a tinted card failure is said with `errorContainer` (a whole banner, held below the tint band) rather than with red text. Anyone adding field validation to a section card has to revisit this | `mobile/lib/theme/app_theme.dart` `_danger`; `mobile/test/theme/contrast_test.dart` "destructive copy is AA on every surface that actually carries it", "the error accent is still findable on a card tint" | open |
| UI-050 | theme | ColorScheme.outline | P3 | theme | n/a | One outline value keeps clearing 3:1 everywhere | `outline` `#8E8B85` sits in a window only 0.076 of relative luminance wide: >= 0.224 to clear 3:1 on the lightest tint (ochre, 3.38:1) and <= 0.300 to stay a step down from white paper (3.40:1), because the same colour is both the control boundary and the document mat ring. It holds only because the eight tints span 1.21:1 in brightness. A lighter tint added to `AppTokens.cardTints`, or a card tier placed above them, closes the window and forces the boundary to be split in two -- one value for controls, one for the mat. Both ends are asserted so the failure is loud, but the coupling is not obvious from either end | `mobile/lib/theme/app_theme.dart` `_outline`; `mobile/test/theme/contrast_test.dart` "the outline is bounded from above as well as below", "the card tints share one luminance band" | open |
| UI-051 | resume-list | _ResumeCard | P3 | polish | 390x844 (flutter_test render) | A list of saved resumes has something to navigate by | A row is tinted by its design's *category*, so a user who keeps four corporate resumes gets four identical slate rows -- the colour is honest (they really are one family) but it stops being a navigation aid at exactly the point a list gets long. Tinting per design instead would put all eight tints in one column, which is the confetti the brief rules out. Left as is; if it bites, the plate rather than the card is the thing to vary | `mobile/lib/screens/resume_list_screen.dart` `_ResumeCard`; `mobile/lib/screens/gallery_screen.dart` `categoryTint` | open |
| UI-052 | app | all | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render) | The matte palette is confirmed on a real device | Verified by rendering the empty and populated home, all six editor sections by scrolling, the disabled end date, the gallery at five viewports, and the preview's busy/stale/loading states to PNG from `flutter test` with the real Inter cuts and the SDK Material icon font, then reading the images. Not shown by that path: OLED rendering of `#0E0E10` at low brightness (a near-black base is where an OLED panel's black smear and colour shift are most visible), real touch and scroll physics, true safe-area insets, the system keyboard, and native text scaling. The requester holds the emulator and was explicit that this task must not launch the app | captures taken through a temporary harness at five viewports, deleted after use | open |

Photo-source and export-destination choosers. UI-053 onwards.

| UI-053 | editor | EditorScreen | **P1** | keyboard | 390x844 at 1x, with and without a 336px IME inset (flutter_test) | The keyboard stays up while the user types | Reported as "after typing a few words the keypad auto hides", diagnosed upstream as the truncation banner being inserted at index 0 of the body `Column`, unmounting the `Expanded` and the whole tab subtree with it. **That mechanism is disproven.** `Element.updateChildren` scans from both ends, and the `Expanded` is the *last* child, so it is matched from the bottom and updated in place: `tester.element(find.byType(TabBarView))` and the form list element are the *same objects* across the banner appearing. Focus, `testTextInput.isVisible`, the input connection and the typed text all survive the transition, with the IME inset applied and a field focused. Not reproduced, therefore not fixed — the real cause is still unknown and is device-only so far. Ruled out from here: no `FocusNode`, `autofocus`, `unfocus` or `requestFocus` anywhere in `lib/`; no plugin on the typing path touches the IME (`printing`'s `rasterPdf` runs on a background thread through `PdfRenderer`, `image_picker` and `flutter_file_dialog` are not on this path). Still open as suspects, all needing a device: the repeated `compute` isolate per render, `Printing.raster` firing on every render for the *offstage* preview tab (`AutomaticKeepAliveClientMixin` keeps it alive), and `EditableText`'s caret-on-screen walking up into the `TabBarView`'s `PageView`. Hardened regardless: the banner is now always a child of the column and draws a zero-height box when there is no loss, so the child count and types are constant by construction | `mobile/lib/screens/editor_screen.dart` body `Column`; `mobile/test/screens/editor_keyboard_test.dart` (4 tests, all of which passed *before* the hardening — they are guards, not a reproduction) | open |
| UI-054 | editor | _ChooserSheet | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test) | Both chooser sheets are seen on a real device | Verified by widget test only: sheet geometry, row height, the 640px tablet cap, both rows' semantics and every outcome path. Never seen as pixels, and three things only a device shows: the real camera and photo-library activities returning to the app, the Storage Access Framework save picker (its dismissed and saved outcomes are driven through a stub here), and how the recessed amber mark reads on an OLED panel inside a `surfaceContainerHigh` sheet | `mobile/test/screens/editor_photo_source_test.dart`, `mobile/test/screens/editor_export_test.dart` | open |
| UI-055 | export | PdfExport.printDocument | P3 | dead-control | n/a | Every shipped capability is reachable | Unchanged from UI-013 — `printDocument` still has no entry point — but the cost of fixing it has dropped: the export destination sheet now exists and takes a list of options, so Print is one `_ChooserOption` plus one `switch` arm. Left out deliberately: the user asked for "save to local disk or share", and a third row nobody asked for would have been the only unrequested thing on the sheet | `mobile/lib/screens/editor_screen.dart` `_chooseExportDestination`; `mobile/lib/render/pdf_export.dart` | open |
| UI-056 | editor | _PhotoTile | P3 | state | n/a | A photo request in flight is visible, not just guarded | A second tap between the sheet closing and the platform picker appearing is dropped by an in-flight flag rather than shown as a disabled button. Deliberate — the platform UI is about to cover the app, and a button that greys out for 200ms and comes back reads as a glitch — but it does mean the only feedback for a slow picker launch is the picker itself. `PhotoService.downscale` also runs on the UI isolate on return; a 12MP capture decodes in the low hundreds of milliseconds on a mid-range phone, unmeasured | `mobile/lib/screens/editor_screen.dart` `_pickingPhoto`; `mobile/lib/data/photo_service.dart` `downscale` | open |

Sample-fixture portrait, and the preview page's own edge. UI-053 onwards.

| UI-053 | gallery | _PageFrame | P2 | polish | 390x844 (flutter_test pixel read of the preview; PyMuPDF raster of the same thirteen PDFs at a 150dp page width) | A document thumbnail is a rectangle, like the page it stands for | `_PageFrame` still wraps its thumbnail in `borderRadius: radiusSm` with `Clip.antiAlias` -- the same clip just removed from the preview's A4 page, and it bites harder here. 12dp of arc on a ~150dp-wide thumbnail is 8% of the page width against roughly 3% on the preview, and nine of the thirteen designs run a full-bleed colour band, sidebar or surface fill into the top edge, so what the arc eats is the design being advertised rather than margin. Not fixed: `lib/screens/` was outside this task's ownership, and the two frames now disagree -- the preview is square, the gallery is not, and the comment in `_PageFrame` still claims "a document is framed identically wherever the app shows one". Only the radius diverged; the `outline` mat ring is identical in both and is what carries the separation | `mobile/lib/screens/gallery_screen.dart` `_PageFrame`; `mobile/test/widgets/resume_preview_test.dart` "nothing clips the corners off the page" | open |
| UI-054 | preview | ResumePreview | P3 | polish | 360x800, 390x844, 430x932, 768x1024, 844x390 (flutter_test render, pixel read at 2x) | The A4 page is a sheet of paper: square corners, and nothing missing from them | The page was drawn with `radiusSm` (12) and `Clip.antiAlias`, so the preview rounded off the corners of the document it exists to show -- reported by the user, and worst on the nine designs whose header band, sidebar or surface fill runs full-bleed into the top edge. Fixed: no `borderRadius`, clip downgraded to `Clip.hardEdge` (kept only so a bottom-anchored stale banner cannot grow off the paper at large text scales), mat ring untouched. This also retires the UI-033 workaround: `spaceMd` was chosen to clear the corner arc and merely happened to equal `radiusSm`; with no arc it is a plain margin off the spacing scale and the two are no longer coupled | `mobile/lib/widgets/resume_preview.dart`; `mobile/test/widgets/resume_preview_test.dart` "the page has square corners, like a sheet of paper", "the page keeps its mat ring against the matte base", "nothing clips the corners off the page" | fixed |
| UI-055 | gallery | _Thumbnail, sampleResume | P2 | tooling | n/a | The portrait in the sample fixture is seen through the app's own raster path | `PdfRaster.firstPageToPng` calls `Printing.raster`, which throws `MissingPluginException` under `flutter_test`, so no test in this repo can look at a rasterised thumbnail (the same wall UI-048 hit). The portrait was therefore verified two steps short of the real thing: the PDFs were built by `PdfRenderer` in a test and rasterised *outside* Flutter with PyMuPDF, at full page size and at a 150dp thumbnail width. What that leaves unseen is the app's own rasteriser at the app's own pixel ratio, the portrait inside a real `_PageFrame` on a real device, and how a light-ground portrait reads on the four designs that place it on a dark header (Beacon, Circuit, Ember, Meridian) on an OLED panel | `mobile/lib/data/sample_portrait.dart`; `mobile/test/data/sample_portrait_test.dart`; captures taken through a temporary harness, then deleted | open |
| UI-056 | templates | Prism | P3 | polish | n/a | A borderless portrait separates from the page it sits on | The generated portrait's backdrop is a warm neutral that measures 1.16:1 at its lightest point and 1.77:1 at its vignetted edge against white paper. Three of the eight designs draw no ring around the portrait at all -- Beacon, Meridian and Prism -- and of those, only Prism puts it on a light surface (Beacon's header band and Meridian's sidebar are dark, where the same ground measures 9.04:1 and 6.22:1 off the rasterised pages). So on Prism the vignette is the entire separation between portrait and header card. Deliberate rather than a defect: a real studio headshot on white paper behaves exactly this way, and painting a hard frame into the *image* would be a border the template never asked for and could not turn off. Logged because it is a measured number that a later re-tone of the portrait could lose by accident. Templates are read-only and were not touched | `mobile/assets/sample/generate_sample_portrait.py` `GROUND_CENTRE`, `GROUND_EDGE`; `mobile/lib/templates/prism/prism_template.dart` `_headerCard` | open |

Home screen and app shell: one create action, a leave prompt, the about screen,
and the header. Numbered from UI-060 — UI-053, UI-054, UI-055 and UI-056 were
each issued twice by concurrent agents above, so those four IDs are ambiguous
and none of the rows above were renumbered.

| UI-060 | resume-list | ResumeListScreen | P3 | interaction | 390x844 (flutter_test render) | One control per destination on the home screen | Reported by the user: "on the landing screen on top and bottom right button buttons do same thing open create screen". UI-028 had already logged it and answered it by making the FAB icon-only, which removed the repeated *label* and left the repeated *control*. Fixed properly: the "New resume" quick-action tile is deleted and the FAB is the sole create action. The FAB is the one that survives because it is the only one still on screen after a scroll — the moment a user with a full list decides to add another — and because it is a fixed graphic at every text size, where the tile grew. The empty state keeps its own labelled CTA, so a first run still gets words. Residual and deliberate: the "Browse designs" card also opens the gallery, framed as browsing rather than creating (the gallery retitles itself for it). That is one destination behind two differently-worded controls, which is the weak form of the same smell; it stays because deleting it would remove the only labelled route to the design catalog, which the user did not ask for. If it is raised again, delete the card — nothing is lost but the framing | `mobile/lib/screens/resume_list_screen.dart` `_BrowseDesignsCard`, `build`; `mobile/test/screens/resume_list_layout_test.dart` "the create action exists exactly once", "the create action outlives a scrolled list" | fixed |
| UI-061 | resume-list | ResumeListScreen | P2 | navigation | 390x844, 360x800 (flutter_test, real `popRoute` platform message) | Back on the root route does not close the app without asking | Reported by the user: "back press should not directly close the app instead ask if user want's to leave". Fixed with `PopScope(canPop: false)` + `onPopInvokedWithResult` (`WillPopScope` is gone from this Flutter), guarded by `ModalRoute.of(context)?.isFirst` so the prompt can only ever belong to the root route — the editor, the gallery, the about screen and both dialogs pop exactly as before, asserted. Confirming calls `SystemNavigator.pop()`, which is Android's exit path; on iOS there is no system back, so the prompt is effectively Android-only. Copy is deliberately non-alarming and makes no claim of data loss, because there is none: "Leave ResumeForge?" / "Your resumes are saved on this device and will be here when you come back." — plain `FilledButton` confirm rather than `confirmDestructive`'s error-coloured one | `mobile/lib/screens/resume_list_screen.dart` `_confirmLeave`; `mobile/test/screens/resume_list_exit_test.dart` (11 tests) | fixed |
| UI-062 | about | AboutScreen | P3 | tooling | n/a | The app's version comes from the build, not from a copy of it | `package_info_plus` is not a dependency and this task could not add one, so `appVersion` in `about_screen.dart` is a const that duplicates `version:` in `pubspec.yaml`. Held in step by a test that reads the manifest off disk (`flutter test` runs from the package root), so the drift is caught rather than shipped — but a release that bumps the pubspec still has to edit two files. The real fix is one dependency and one `PackageInfo.fromPlatform()` await | `mobile/lib/screens/about_screen.dart` `appVersion`; `mobile/test/screens/about_screen_test.dart` "the version const matches pubspec.yaml" | open |
| UI-063 | about | AboutScreen | P3 | polish | n/a | The developer credit points somewhere | "Developed by codevioso" is a plain statement with no link, because no URL, contact address or legal entity was supplied and inventing one would put a wrong destination in shipped UI. A dead or placeholder-looking control would be worse, so the placeholder is a `TODO(codevioso)` in the source rather than anything on screen: one `TextButton` in the credit card is all it needs when a real destination exists | `mobile/lib/screens/about_screen.dart` (TODO above `AboutScreen`) | open |
| UI-064 | resume-list | _TopBar | P3 | responsive | 360x800 at 2x text (flutter_test render, real Inter) | The product name is never cut in half | The title row is one unbreakable word beside a fixed 48px icon button, so at large accessibility text sizes the only move left is to ellipsize. `Wordmark` therefore clamps its own text scaler at 1.6x (`titleLarge` 22 -> 35.2), which is the last size that fits "ResumeForge" plus the button on a 360px phone with the real typeface. Capping accessibility scaling is a deliberate exception and applies to nothing else on the screen; if the product name ever gets longer, this number is the thing that breaks first. **Fixed by the rename**: "Resivo" is six characters where "ResumeForge" was eleven — ~67px at 1x against the 288px this row leaves on a 360px phone, ~200px at 3.1x (the largest iOS accessibility size) — so the clamp and its constant are deleted and the title scales with the system like everything else. The test no longer asserts the mechanism: it asserts the outcome the clamp was buying, that the paragraph never reports `didExceedMaxLines` and the button stays inside the screen, which is what will fail first if the name grows again | `mobile/lib/screens/resume_list_screen.dart` `Wordmark`; `mobile/test/screens/resume_list_layout_test.dart` "the title shares its row at 2x text on a small phone", "the name is not ellipsized even at an extreme text size" | fixed |
| UI-065 | resume-list | ResumeListScreen | P3 | responsive | 800x360 (flutter_test, measured) | A landscape phone still builds the rows it can reach | The title row sits outside the scroll view so the product name and the information button exist in every state and survive a scroll, which costs the list 56px of viewport. On an 800x360 landscape phone that is the difference between three rows being built ahead of time and two: the third now falls outside the viewport's 250px cache extent and is built on scroll, which is a lazy list working as designed but is a real (small) reduction in what is on screen at once. Portrait is unaffected (~7% of the viewport). Accepted for a persistent title row; the alternative is scrolling it away, which loses the information button on a long list | `mobile/lib/screens/resume_list_screen.dart` `build`, `_TopBar`; `mobile/test/screens/resume_list_layout_test.dart` "long titles at phone landscape" | open |
| UI-066 | resume-list, about | ResumeListScreen, AboutScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render) | The header, the about screen and the leave prompt are seen on a real device | Verified by rendering both screens plus the leave prompt to PNG from `flutter test` with the real Inter cuts and the SDK Material icon font, then reading the images; the harness was deleted afterwards. That path cannot show: the real status-bar inset above the title row (the capture has none, so the top padding is only reasoned about), an actual Android back gesture or back button reaching `PopScope`, `SystemNavigator.pop()` genuinely closing the activity, and how the title row reads with a notch or a punch-hole camera over it. This task was explicitly asked not to launch the app | captures taken through a temporary harness at six viewports, deleted after use | open |
| UI-067 | app-wide | Wordmark, SplashScreen | P3 | polish | n/a | The product is named in exactly one place | The rename to Resivo introduced `appName` in `about_screen.dart`, which the home title row, the leave prompt, the information button's tooltip and the about heading all read. The splash screen keeps its own `'Resivo'` string literal instead, so the product name now exists twice in Dart and a second rename has two places to reach. Not fixed here: `lib/screens/splash_screen.dart` belongs to the concurrent branding task and was being edited while this ran. The fix is one import and one substitution — the splash wordmark reads `appName` | `mobile/lib/screens/about_screen.dart` `appName`; `mobile/lib/screens/splash_screen.dart` (wordmark `Text`, tagline) | open |
| UI-068 | about | AboutScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render, real Inter + SDK Material icons) | The vendor logo is seen decoding on a real device | The credit now carries `assets/vendor/codevioso-logo-white.webp` rather than plain text. Verified by capture: correct aspect at every viewport, white on plum at 13.92:1, capped at 1.6x text and still inside a 360px card. What the capture cannot show is the *arrival*: in `flutter_test` the asset had to be `precacheImage`d inside `runAsync` before it would paint at all, so the frame between "box reserved" and "mark drawn" was never observed on hardware. The box is reserved at the right size from one number (width leads, height follows), so there is no layout jump to see — but the fade-in, the decode cost on a low-end phone, and the `errorBuilder` fallback in a real failure are all unwitnessed. The fallback is exercised as a function by a test, not by an actual decode failure | `mobile/lib/screens/about_screen.dart` `_DeveloperLogo`; `mobile/test/screens/about_screen_test.dart` "the mark is drawn as supplied", "a mark that cannot be drawn still credits them" | open |

Editor save legibility (QA-9) and the month/year picker (QA-7). Numbered from
UI-080 to stay clear of the concurrent template/URL task, which was holding the
UI-069+ range while this ran.

| UI-080 | theme | AppTheme.chipTheme | P3 | theme | 360x800 at 1x and 2x (flutter_test pixel read) | Every chip in the app is lettered in Inter | `chipTheme.labelStyle` is a bare `TextStyle(color: onSurface)`, and a bare `TextStyle` drops `ThemeData.fontFamily` — the exact trap `filledButtonTheme`, `outlinedButtonTheme` and `textButtonTheme` each carry a comment about. Any chip that does not pass its own `labelStyle` therefore letters in the fallback face: Roboto on device, and nothing bundled at all on web. Found by rendering the new month picker, whose twelve chips came back as blank boxes. Not fixed here — `lib/theme/` was outside this task's ownership — so the picker works around it locally, exactly as `_CategoryChip` in the gallery already does. Two workarounds now exist for one theme bug; the fix is `base.textTheme.labelLarge?.copyWith(color: …)` in `chipTheme` and deleting both | `mobile/lib/theme/app_theme.dart` `chipTheme`; `mobile/lib/widgets/form_fields.dart` `_chipLabelStyle`; `mobile/lib/screens/gallery_screen.dart` `_CategoryChip` | open |
| UI-081 | editor | EditorScreen | P2 | state | 390x844 (flutter_test) | Leaving by *any* route flushes the pending write and says so if it fails | Done now awaits `saveNow()` before popping and refuses to leave on a failed write. The system back gesture, the back arrow and a swipe-back still go through `dispose()`, which calls `saveNow()` fire-and-forget: the write is started but nobody waits for it and nobody hears it fail, so a resume abandoned mid-debounce on failing storage is lost silently. Not fixed: closing it means a `PopScope` on the editor route, and the root route's leave-prompt guard (UI-061) already lives on the same mechanism one screen away, so the interaction between the two deserves its own pass rather than a bolt-on here | `mobile/lib/screens/editor_screen.dart` `dispose`, `_done`; `mobile/test/screens/editor_save_state_test.dart` "flushes what is pending before the list can be seen" | open |
| UI-082 | editor | EditorScreen app bar | P3 | responsive | 360x800 (flutter_test render, real Inter) | The document title is readable on the smallest supported phone | With a back arrow and four actions (design, export, Done, overflow) the title slot on a 360px phone is ~104px, so "Amara Okonkwo" renders as "Amara Ok…". The save state does not cost any of that — it is stacked under the title, which is why it is stacked — but the Done action does. Accepted: the name is orientation rather than information (the user just picked this resume off the list), and a labelled `TextButton` in its place would take ~74px instead of 48px. It fits in full from 430px up | `mobile/lib/screens/editor_screen.dart` `_EditorTitle`; captures at 360/390/430/768 | open |
| UI-083 | editor | AppToast over the app bar | P3 | polish | 390x844 (flutter_test render) | A message does not cover the thing it is about | The failed-save toast is anchored top-right in the overlay, directly over the app bar — so while it says "Could not save this resume", it hides the "Not saved" indicator it is explaining. Every toast in the app has always sat there and this is the first one whose subject is *behind* it. Not fixed: moving the toast layer is an app-wide change to `AppToast`, and the indicator is visible again the moment the toast is dismissed or times out | `mobile/lib/widgets/app_toast.dart` `_ToastLayer`; `mobile/lib/screens/editor_screen.dart` `_done` | open |
| UI-084 | editor | EditorScreen, MonthYearField | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render, real Inter + SDK Material icons) | The indicator, the Done action and the picker are seen on a real device | Verified by rendering the editor, both save states, the failure state, the picker sheet and the year list to PNG from `flutter test` and reading the images; the harness was deleted afterwards. `flutter devices` lists Windows, Chrome and Edge only — no Android SDK, emulator, `adb` or iOS simulator in this environment — and this task was asked not to launch the app. Unseen: real touch on 32px-tall chips (the tap target is 48 and is asserted, but only as geometry), the sheet's drag-to-dismiss on hardware, the system keyboard actually closing when the picker opens (`FocusScope.unfocus` is called; the IME was faked), and native text scaling | captures taken through a temporary harness at five viewports, deleted after use; `mobile/test/screens/editor_save_state_test.dart`, `mobile/test/widgets/form_fields_test.dart` | open |

| UI-085 | templates | all 13 | P3 | interaction | n/a (PDF inspected with PyMuPDF) | A recruiter reading the PDF on screen can click through to the LinkedIn profile or the project repository | Every URL is drawn as plain text. `dart_pdf` supports link annotations (`pw.UrlLink`), so making them clickable is an annotation change rather than a layout one, but it is a product call — worth doing if PDFs are read on screen, pointless if printed, and it interacts with the display form now applied (the visible text has no scheme, so an annotation would have to carry the original URL). Raised as QA-12 and deliberately left out of the URL-rendering pass, which fixed the display problem regardless of the answer. **Answered and fixed** — the product call was yes; see URL rule item 7 and UI-110..UI-113 | `docs/QA_TASKS.md` QA-12; `lib/templates/template.dart` `urlText`, `urlTarget`; `mobile/test/templates/link_target_test.dart` | fixed |

| UI-086 | templates | Quill Academic, Terminal Green | P3 | overflow | n/a (PDF inspected with PyMuPDF) | A long profile URL is shortened no harder in these two designs than in the other eleven | Both pages are already full at the sample content, so their contact strip is capped at one line (`contactStrip(maxLines: 1)`) and a pathological URL is elided to roughly 22 characters rather than the ~50 the other designs allow. The alternative is worse and was measured: letting the strip take a second line costs Quill its whole Competencies section and Terminal its Education block, because dart_pdf's Column deletes the child it cannot fit. The real fix is UI-016 / UI-010 — page overflow — not the URL rule | `lib/templates/quill/quill_template.dart`, `lib/templates/terminal/terminal_template.dart`; guarded by `test/templates/url_rule_test.dart` "long URLs do not push a section off the page" | open |

| UI-087 | templates | all 13 | P2 | tooling | n/a — PDFs rasterized with PyMuPDF outside Flutter | URL rendering confirmed through the in-app preview on a device | `Printing.raster` throws `MissingPluginException` under `flutter_test`, so the preview path could not be exercised. Verification was done by building the real exported PDF bytes for all thirteen designs against realistic, pathological and empty content, rasterizing them to PNG with PyMuPDF, and reading the images plus extracted word boxes. That covers the document — which is the single source of truth for the preview — but not the on-screen preview, its scaling, or any device rendering | `test/templates/url_rule_test.dart`; harness deleted after use | open |

Skills defects (D-1..D-5, QA-15 of `docs/TEMPLATE_AUDIT.md`). Numbered from
UI-090 to stay clear of the concurrent editor and catalog tasks holding UI-080+.

| UI-090 | templates | Orchid Editorial | **P1** | overflow | n/a — PDFs rasterized and text-extracted with PyMuPDF outside Flutter | A four-role resume still shows where the person studied | Orchid drops its **entire Education section** as soon as a resume has four roles. Its main column is `Experience → Selected work → Education` as plain siblings of one page-height Column, so dart_pdf stops laying out at the first entry that does not fit and paints nothing after it. Reproduced on `sampleResume` plus one ordinary fourth role, with five skills — nothing pathological, and nothing to do with skills: the same page at three roles prints Education fine, and at 24 skills it is no worse. **Not fixed:** this task's remit was the skills defects, and Orchid has none. The fix is the mechanism now in `template.dart` and already applied to Quill, Linen and Terminal — put `_column`'s experience and projects in a `pw.Flexible(fit: FlexFit.loose)` and bind each `_sectionLabel` to its first entry with `headedSection`, which is roughly ten lines. Note this also means the audit's D-4 claim that "every other template caps at 8–16 so its sidebar cannot be monopolised this way" is true of skills but not of the section it was protecting | `mobile/lib/templates/orchid/orchid_template.dart` `_column`; `mobile/test/templates/skills_test.dart` "a long list of skills does not evict education" deliberately holds the role count at three so it cannot absorb this | open |
| UI-091 | templates | Prism Cards | P2 | accessibility | n/a — ratios computed from the palette constants, confirmed against the rasterized page | Every run of text on the page clears WCAG AA | The filled skill chip was the reported defect (D-5, white on `#8B5CF6` = **4.23 : 1**) and is fixed — the chip now fills `#7C3AED`, **5.70 : 1**. But 4.23 : 1 is the ratio of `palette.primary` against **white**, so the same colour fails wherever else Prism sets text in it on a white card: the job title (11.2 pt semibold), every company name (9.2 pt semibold), and the project technology chips (7.6 pt on the `#F5F3FF` accent, which is worse again at 3.86 : 1). None is large text, so all three need 4.5 : 1. **Not fixed:** the brief scoped D-5 to the skills chip, and moving `primary` itself re-tones the whole design — the title, the borders and the card rules — which is a design decision rather than a defect fix. Moving `primary` to `#7C3AED` would clear all of them at once and is what `strongChipFill` is already set to | `mobile/lib/templates/prism/prism_template.dart` `palette`, `_headerCard`, `_experience`, `_projectCard`; ratio guarded for the chip only by `mobile/test/templates/skills_test.dart` "prism draws white on a violet that clears AA" | open |
| UI-092 | templates | 11 chip and meter designs | P3 | polish | n/a — PDFs text-extracted with PyMuPDF | A design that shows fewer skills than the user entered says so | Skills rule item 4 gives the `+N more` marker to Quill and Linen only, because those two set skills as one continuous run where a lost name leaves no gap and no mark at all. The other eleven enumerate discrete chips or meters and simply stop at their `take(n)` cap — 8 on Meridian and Ledger, 16 on Circuit and Prism — with nothing on the page saying more were entered. Deliberate, and recorded as a judgement in the rule rather than an oversight: a reader can see the shape of a chip cloud, the caps are a declared density decision the audit classifies as design variation, and adding a marker chip to eleven designs is both a homogenising change and one more block of height for dart_pdf to evict something over. The user is still warned at export — `TruncationCheck` reports `skills` whenever the cap bites, verified at the 10/11/12/13-skill boundary | `mobile/lib/templates/template.dart` "THE SKILLS RULE" item 4 | open |
| UI-093 | templates | all 13 | P2 | tooling | n/a — PDFs rasterized with PyMuPDF 1.28.2 outside Flutter, at 2x–7x | The skills fixes are confirmed through the in-app preview on a device | `Printing.raster` throws `MissingPluginException` under `flutter_test`, so nothing in this repo can look at a rasterized page. Verification was done by building the real exported bytes for all thirteen designs across six content fixtures, rasterizing them outside Flutter, and reading the pixels — including a whole-page pixel diff of the shipped sample before against after, which came out identical on twelve of thirteen (Prism differs only in the intended chip colour). That covers the document, which is the single source of truth for the preview, but not the on-screen preview, its scaling, or any device rendering. No Android SDK, emulator, `adb` or iOS simulator in this environment, and this task was asked not to launch the app | `mobile/test/templates/skills_test.dart`; harness deleted after use | open |

Phone field with a country picker (QA-6) and the photo-free disclosure (QA-10).
Numbered from UI-100 to stay clear of the concurrent template tasks holding
UI-085+ and UI-090+.

| UI-100 | editor | PhoneField country sheet | P2 | overflow | 800x360 at 1x, and at 2x text (flutter_test render, real Inter) | The country picker opens on a landscape phone without clipping | Shipped in this task's first build and caught by rendering it: the sheet was a `Column` of heading, search field, current-country row, "No country code" row, divider and a `Flexible` list, and a landscape sheet is 258px tall — the fixed siblings alone overflowed it by 14px while the `Flexible` list shrank to nothing. **Fixed** by moving the two leading rows into the list itself, so everything below the search scrolls together and a short viewport scrolls rather than clips. Regression-tested at 800x360 and 768x1024 at 1x and 2x, with the text size pushed through `platformDispatcher` because a modal route does not inherit a `MediaQuery` pushed in around the widget under test | `mobile/lib/widgets/form_fields.dart` `_PhoneCountrySheetState.build`; `mobile/test/widgets/form_fields_test.dart` "the country picker opens at …" | fixed |
| UI-101 | editor | PhoneField code control | P3 | polish | 390x844 (flutter_test render, real Inter) | The control shows the dial code it holds, not an ellipsis of it | Measured to the pixel, the control's box came out exactly as wide as `+880` needs and Material ellipsized it to `+…` — the one string a country-code control must never show. **Fixed** by rounding the measured width up by one spacing step. Found by capture, not by a test: nothing asserts an ellipsis, because the widget reports no overflow when it draws one | `mobile/lib/widgets/form_fields.dart` `_PhoneFieldState._codeWidth` | fixed |
| UI-102 | templates | ResumeTemplate | P2 | state | n/a — source scan under `flutter test` | Which designs draw a portrait is a fact the templates state about themselves | The gallery cue and the editor's photo note both read `photoFreeTemplateIds` in `gallery_screen.dart`, which is a UI-side *mirror* of what the templates do. The real signal is which template calls `tryDecodePhoto`, and nothing on `ResumeTemplate` exposes it. Held in step by `template_photo_support_test.dart`, which derives the set from the source of `lib/templates/` and fails naming the design that changed — so it cannot ship stale, but it is still two places. The fix is a `usesPhoto` (or `drawsPortrait`) getter beside `requiredFonts`, which deletes the source scan and the mirror; `lib/templates/` was outside this task's ownership | `mobile/lib/screens/gallery_screen.dart` `photoFreeTemplateIds`; `mobile/test/screens/template_photo_support_test.dart` | open |
| UI-103 | editor | PhoneField | P3 | interaction | 390x844 (flutter_test render) | Someone who never leaves the phone field still hears what the check found | `checkPhoneNumber`'s note appears when focus leaves the field, so a user who types an implausible number and goes straight to Export never sees it. Deliberate — shown while the caret is still in the field it fires on the keystroke after "12", which is scolding someone mid-number — and harmless by construction: the check is a plausibility check, never a gate, and the number is saved and exported either way. If it needs to be louder, the place is the export dialog, beside the truncation warning, not the field | `mobile/lib/widgets/form_fields.dart` `_PhoneFieldState._note` | open |
| UI-104 | editor | PhoneField country sheet | P3 | polish | 390x844 (flutter_test render) | The country already in use is visible without hunting for it | The 246-row list does not scroll to the current country, because `ListView.builder` has no fixed row height to compute an offset from at every text size and a `GlobalKey` cannot reach a row that has not been built. Worked around by pinning the current country as the first row, labelled "Currently in use", so the state is visible even though its place in the alphabet is not. A `ScrollablePositionedList`-class dependency would do it properly; this app ships a deliberately short dependency list | `mobile/lib/widgets/form_fields.dart` `_PhoneCountrySheetState.build` | open |
| UI-105 | editor | PhoneField | P2 | responsive | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x (flutter_test render, real Inter) | The code/number pair is measured against the font the device actually letters in | The pair stacks when the code control and the country's example number cannot share a line, measured with `measureText`. Under `flutter_test`'s font every glyph is a full em box, so an example number measures about twice its real width and the pair stacks at widths where a device with Inter loaded still fits both. Captures with the real font confirm the device behaviour (side by side at 360–768 at 1x, stacked at 360 at 2x), but the *threshold* itself cannot be asserted in the suite without asserting the test font — so the layout tests assert the property (whichever arrangement, the number keeps the width and the two do not collide) with only the unambiguous ends pinned. One existing test had to be given a taller viewport for the same reason: `editor_quick_actions_test` sized its viewport to the form as it stood | `mobile/lib/widgets/form_fields.dart` `_PhoneFieldState.build`; `mobile/test/screens/editor_phone_test.dart`; `mobile/test/screens/editor_quick_actions_test.dart` `tall` | open |
| UI-106 | editor, gallery | PhoneField, EditorScreen, GalleryScreen | P2 | tooling | 360x800, 390x844, 430x932, 768x1024, 800x360 at 1x and 2x text (flutter_test render, real Inter) | The phone pair, the country sheet and the photo note are seen on a real device | Verified by rendering the editor form, the country sheet (default, guessed `+1`, landscape, and 2x), the soft note, the photo disclosure and the gallery cues to PNG from `flutter test` with the real Inter cuts, then reading the images; the harness was deleted afterwards. `adb` is not on PATH and this task was asked not to launch the app, so nothing was seen on Android or iOS. Unseen: the real phone keyboard opening under the sheet, drag-to-dismiss on hardware, native text scaling, and the SDK Material icon font — icons render as boxes in these captures, so the chevron, the search glyph and the note's mark were verified as geometry rather than as artwork | captures taken through a temporary harness at six viewports, deleted after use | open |
| UI-107 | gallery | GalleryScreen | P3 | polish | 390x844 (flutter_test render) | The gallery says a design ignores a photo *the user has actually added* | The cue under every thumbnail now names each design's stance on a portrait ("Photo" / "No photo") for all thirteen, which is a permanent label and is read by whoever reads cues. What it is not is contextual: the gallery is pushed from the editor, which knows whether a photo exists, and could mark the five photo-free cards more loudly when one does. Left out deliberately — the editor's own note already covers the contextual case, and it appears where the photo is rather than one screen away | `mobile/lib/screens/gallery_screen.dart` `templateCues`, `photoFreeTemplateIds` | open |

Link annotations (QA-12) and field marks (QA-8). Numbered from UI-110 to stay
clear of the phone-field and photo-disclosure task holding UI-100+.

| UI-110 | templates | all 13 | P3 | interaction | n/a — PDFs inspected with PyMuPDF 1.28.2 outside Flutter | A URL field the user filled with something that is not a URL behaves predictably | `urlTarget` links a scheme-less string only when what precedes the first `/ : ? #` looks like a hostname — dot-separated labels ending in two or more letters — so `amara.design` becomes `https://amara.design` while `n/a`, `available on request`, `Ph.D` and `192.168.1.1` are drawn as text with **no annotation at all**. Deliberate, and recorded here because it will read as a defect to whoever tests it: "the link doesn't work" for `n/a` is the rule working. The alternative — prefixing `https://` unconditionally — produces a link to nonsense that a reader cannot tell is broken until they follow it, which is worse than plain ink. Non-http(s) schemes (`mailto:`, `javascript:`, `data:`, `file:`, `ftp:`) are also drawn and not linked | `mobile/lib/templates/template.dart` `urlTarget`, URL rule item 7; `mobile/test/templates/link_target_test.dart` "refuses anything that is not an address" | open |
| UI-111 | templates | all 13 | P3 | polish | n/a — annotation rectangles read with PyMuPDF | The tappable area of a wrapped URL follows its glyphs | A link annotation is one rectangle, and a URL that wraps to two or three lines in a narrow rail (Aurora, Circuit, Meridian, Compass, Orchid) gets a single rect over the whole block. Where the last line is short, the hot area extends past its final glyph to the width of the longest line. Harmless — nothing else is drawn inside that box, verified for all 156 annotations across three fixtures — and the alternative is per-line `pw.TextSpan(annotation:)` inside a `pw.RichText`, which trades the one shared `urlText` code path for a second text-building path in the helper the URL rule depends on. Not worth it for a few points of dead margin | `mobile/lib/templates/template.dart` `urlText` wraps `pw.UrlLink` around the whole `pw.Text` | open |
| UI-112 | templates | 12 of 13 | P3 | polish | n/a — stroked-path counts read from inflated content streams | QA-8's "an icon for each field in all templates" | Answered as one design, not thirteen: **Aurora only**. Quill, Ledger, Linen, Orchid and Terminal are deliberately typographic; Meridian and Compass already name each contact field in words, and an icon there would either duplicate the label or replace machine-readable text with vector art in a document that is frequently ATS-parsed; Beacon, Coral, Ember and Prism set contact as one flowing `contactStrip` run where a mark reads as noise between separators; Circuit is stacked and unlabelled but its vocabulary is the mono rule and the solid square, not a pictogram. Coral and Prism are the closest to a second candidate and were **deferred**, not rejected: a mark per run means extending `contactStrip`'s width bisection — the helper that fixed QA-4 — with a fixed per-run prefix, which is not a trade worth making in the same pass that added link annotations. Pinned by a test so the answer cannot drift silently in either direction | `mobile/lib/templates/template.dart` "THE FIELD MARK RULE"; `mobile/test/templates/field_mark_test.dart` "exactly one design in the catalog draws field marks" | open |
| UI-113 | templates | all 13 | P2 | tooling | n/a — PDFs built and inspected with PyMuPDF 1.28.2 outside Flutter | The links and the Aurora marks are seen in the in-app preview on a device | `Printing.raster` throws `MissingPluginException` under `flutter_test`, so nothing in this repo can rasterize a page. Verification was the exported bytes for all thirteen designs across five content fixtures: every `/Link` annotation listed, its target compared against the field the user typed, and its rectangle checked against the word boxes underneath it (156 annotations, 0 covering anything but their own URL); Aurora's contact block read at 9x–12x. That covers the document, which is the single source of truth for the preview, but not the preview widget, its scaling, or any device rendering — and **notably not whether a PDF viewer on Android or iOS actually opens the link on tap**, which is the whole point of QA-12. `flutter devices` lists Windows, Chrome and Edge only; no Android SDK, emulator, `adb` or iOS simulator, and this task was asked not to build or run the app | `mobile/test/templates/link_target_test.dart`, `mobile/test/templates/field_mark_test.dart`; harness deleted after use | open |

Email and phone link annotations (QA second round). Numbered from UI-120 to stay
clear of the link-annotation and field-mark pass holding UI-110+.

| UI-120 | templates | all 13 | **P1** | tooling | n/a — annotation dictionaries read from the exported bytes | The suite is green with email and phone linked | **Blocker, not fixed, needs a decision.** `mobile/test/templates/link_target_test.dart` predates email and phone being linkable, and five of its assertions are scoped to *every annotation on the page* rather than to URL annotations. All five are now false by construction, and 53 of its 114 tests fail: `every annotation is a borderless http(s) link` (line 349, `^https?://` on every target), `draws one annotation per URL, never a duplicate` (line 371, `lessThanOrEqualTo(4)` — the sample resume now has six linkable fields), `draws no annotation for a blank or whitespace URL` (line 313) and `draws no annotation for text that is not a URL` (line 325) — both blank only the three URL fields while the sample's email and phone stay populated — and `no design links anything the user did not type` (line 409), whose `allowed` set is built from `urlTarget` alone. None is a display or layout assertion; `url_rule_test.dart` (117) and `catalog_test.dart` pass untouched, which is what proves nothing moved. **The task forbade editing the file, so it was not edited.** The change needed is to scope those five to `where((t) => t.startsWith('http'))`, raise the cap to 6, and add `emailTarget`/`telTarget` to the allowed set — or to delete them, because `contact_link_test.dart` was deliberately written as a strict superset of all five | `mobile/test/templates/link_target_test.dart` lines 313, 325, 341–360, 361–372, 409–427; superseding coverage in `mobile/test/templates/contact_link_test.dart` | open |

| UI-121 | templates | all 13 | P3 | interaction | n/a — PDFs inspected with PyMuPDF 1.28.2 outside Flutter | A phone number with an extension is tappable like any other | `telTarget` refuses any value that is not reducible to digits without loss, which excludes a trailing extension: `+1 (415) 555-0134 x210` is drawn in full and left **unlinked**. Deliberate, and recorded here because it will read as a defect to whoever tests it. Taking "every digit" of it dials `…0134210`, a different subscriber, and a link that calls the wrong person is worse than no link. RFC 3966 spells it `tel:+14155550134;ext=210`, but dialler support for the parameter is uneven — Android treats `;` as a dial-string wait and would drop the rest — so the honest options were "wrong number", "maybe-works parameter" and "no link", and the third was chosen. The email beside it is unaffected: one refusal is not five. Revisit if `;ext=` support becomes dependable | `mobile/lib/templates/template.dart` `telTarget`, `_dialable`, CONTACT LINK RULE item 5; `mobile/test/templates/contact_link_test.dart` "refuses a number carrying an extension", "leaves a phone number with an extension unlinked" | open |

| UI-122 | templates | all 13 | P3 | interaction | n/a — PDFs inspected with PyMuPDF 1.28.2 outside Flutter | Every legal email address is tappable | `emailTarget` accepts a local part of alphanumeric runs joined by single `.`, `_`, `+`, `-` or `'`. RFC 5322 also permits `&`, `%`, `/`, `=`, `!`, `?`, `#` and more, and those addresses are drawn but **not linked**. Deliberate: every character in the accepted set is one RFC 6068 lets a `mailto:` carry literally, so the target is the address verbatim with no escaping step to get wrong — `&` and `%` would need `%26`/`%25` and `?` would start a header section. The rejected forms are vanishingly rare on a resume, and the failure mode of guessing wrong (a link that opens a compose window addressed to something else) is worse than plain ink. Widening this means adding a mailto-specific escaper and testing it, not relaxing the regex | `mobile/lib/templates/template.dart` `emailTarget`, `_emailAddress`, CONTACT LINK RULE item 4; `mobile/test/templates/contact_link_test.dart` "refuses the legal characters it cannot carry literally" | open |

| UI-123 | templates | all 13 | P2 | tooling | n/a — PDFs built under `flutter test` and inspected with PyMuPDF 1.28.2 outside Flutter | Tapping the address or the number in a PDF viewer on a device actually opens mail or the dialler | Same ceiling as UI-113, and it is the one that matters here. `Printing.raster` throws `MissingPluginException` under `flutter_test`, so nothing in this repo rasterizes a page. Verification was the exported bytes for all thirteen designs across five fixtures: every `/Link` annotation listed with its `/Rect`, the target compared against the field the user typed, and the words underneath each rectangle read from `page.get_text("words")` — 78 annotations on the sample fixture, every `mailto:` rect over the address and nothing else, every `tel:` rect over the number and nothing else. That proves the document. It does **not** prove that a viewer on Android or iOS honours `mailto:` and `tel:` from a PDF annotation, which is the whole point of the request, and viewer behaviour for non-http schemes is less uniform than for `https:`. `flutter devices` lists Windows, Chrome and Edge only; no Android SDK, emulator, `adb` or iOS simulator, and this task was asked not to run or build the app | `mobile/test/templates/contact_link_test.dart`; PyMuPDF harness deleted after use | open |

| UI-124 | templates | all 13 | P3 | polish | n/a — glyph positions resolved from the content streams | Adding a link annotation does not move anything on the page | Confirmed rather than assumed, and the method is worth keeping: `pw.UrlLink` paints its child inside a `q … cm … Q`, so a wrapped widget's own coordinates become *relative* and the content stream is no longer byte-comparable with the unwrapped one even though the page is identical. A byte diff of the streams therefore reports a false positive, and a byte diff of the files always does because the extra object renumbers the xref. `contact_link_test.dart` interprets the stream instead — tracks the CTM stack and resolves every glyph run to its absolute point — and compares those. Noted so a future pass does not "simplify" it back into a byte compare and either get a red suite or, worse, weaken it to a length check | `mobile/test/templates/contact_link_test.dart` `glyphRuns`, "contactLink is layout-transparent" | fixed |

| UI-125 | render | PreviewEngine | P3 | tooling | n/a — `flutter test` | `preview_engine_test.dart` passes in the full suite as reliably as it does alone | `coalesces edits arriving mid-render into exactly one follow-up` failed once during a full-suite run and passes on every isolated run. Timing-sensitive and unrelated to this pass — nothing in `lib/render/` was touched — but recorded because a flake that only appears under full-suite load is the kind that gets blamed on whatever change is in flight | `mobile/test/render/preview_engine_test.dart` | open |

The portrait on a resume row (list screen). Numbered from UI-130 to stay clear
of the template passes holding UI-110+ and UI-120+.

| UI-130 | list | ResumeListScreen | P3 | tooling | Windows host, `flutter_tester` renderer at dpr 3 — 360x800, 390x844, 430x932, 768x1024, 800x360 landscape, 1x and 2x text | The portrait plate is seen on a phone | No emulator, device or simulator was available and this task was asked not to run or build the app, so "visually verified" here means PNG frames captured from the real widget tree through `RenderRepaintBoundary.toImage` with Inter and MaterialIcons loaded via `FontLoader`, then looked at. That renders the same Skia the device does and proved the four states (photo, photo-free design, no photo, undecodable bytes) side by side, but it is not a device: it says nothing about touch, about scroll smoothness with many photo rows, or about how the plate reads on a real panel at real size. The decode-cost claim is asserted in a test (`ResizeImage` height ~238 at dpr 3 against a 512px stored square) rather than profiled on a handset | `mobile/test/screens/resume_list_layout_test.dart` "the plate carries the portrait the design will print"; capture harness deleted after use | open |

The photo picker on a design that prints no photo. Numbered from UI-140 to
stay clear of the list-screen pass holding UI-130+.

| UI-140 | editor | EditorScreen `_changeTemplate` | P3 | navigation | Windows host, `flutter_tester` renderer at dpr 3 — 390x844 | Changing design from the editor shows the result of the change | Picking a design pops the gallery and jumps to the **Preview** tab, so the About-you tile appearing or disappearing happens on a tab the user is not looking at; they see it on returning to Edit. Deliberate — switching a design and being left on the form reads as nothing having happened — and recorded because it will read as a defect to whoever tests the picker's disappearance ("the picker didn't come back") and because the transition tests have to make the same round trip to observe it | `mobile/lib/screens/editor_screen.dart` `_changeTemplate`; `mobile/test/screens/editor_photo_tile_test.dart` `switchDesign` | open |

| UI-141 | editor | `_PhotoTile` | P3 | accessibility | Windows host, `flutter_tester` renderer at dpr 3 — 360x800 at 2x text | The tile scales as one thing at large text sizes | The avatar stays 64px and the icons inside the tile stay 18px while the type doubles, so at 2x the marks are small relative to the words beside them and the copy column left over beside the avatar is about 180px on a 360px phone. The disclosure was moved to the tile's full width for exactly this reason; the picker state's two short lines still sit in the narrow column, which is legible but tight. Scaling avatar and icon glyphs with `MediaQuery.textScalerOf` is the fix, and it is an app-wide decision (every icon in this app is a fixed `size:`), not a change to make inside one tile | `mobile/lib/screens/editor_screen.dart` `_PhotoTile._avatarSize`, `_keptHeading` | open |

| UI-142 | editor | `_PhotoTile` | P2 | tooling | Windows host, `flutter_tester` renderer at dpr 3 — 360x800, 390x844, 768x1024, 844x390, 1x and 2x text | The three photo states are seen on a phone | Same ceiling as UI-130. No emulator, device or simulator was available (`adb` is not on PATH, `flutter devices` lists desktop and browsers only) and this task was asked not to run or build the app, so "visually verified" means PNG frames captured from the real widget tree through `RenderRepaintBoundary.toImage` with Inter and MaterialIcons loaded, then looked at. That is the same Skia a device draws with and it caught two real defects — a stranded accent icon and a "Change de…" button label at 2x — but it says nothing about touch targets on hardware, about the platform photo picker sheet, or about how the disclosure reads on a real panel | capture harness deleted after use; `mobile/test/screens/editor_photo_tile_test.dart` holds the assertions it was used to find | open |

<!--
Row template:

| UI-001 | editor | EditorPage | P1 | keyboard | 360x800 (web/chrome) | Submit stays reachable with keyboard open | Submit hidden behind keyboard | console: no overflow; screenshot | open |
-->
